enum RoomType { group, single, admin }

enum EmotionType { like, laugh, angry, wow, heart, sad }

enum SendMessageType { typing, sending, sent, rejected }

enum MessageType { attachment, text, location }
